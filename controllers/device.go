package controllers

// Device controller handlers live on Server in server.go: register, status,
// fcmToken, deleteDevice, and deviceScoped. The file exists so the backend
// folder layout mirrors doc/UPDATE_SERVER.md while the current stdlib router
// keeps route dispatch centralized.
